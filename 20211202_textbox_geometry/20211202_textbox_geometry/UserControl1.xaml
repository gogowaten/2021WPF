<UserControl x:Class="_20211202_textbox_geometry.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:_20211202_textbox_geometry"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200">
  <Grid>
    <TextBox x:Name="MyTextBox" Margin="{Binding ElementName=MyPath, Path=StrokeThickness}"
               BorderThickness="0" Padding="2" BorderBrush="Transparent"
               Text="{Binding Text}"
             HorizontalAlignment="Center" VerticalAlignment="Center"/>
    <Path x:Name="MyPath"
          StrokeThickness="{Binding StrokeThickness}"          
          Stroke="Red" HorizontalAlignment="Center" VerticalAlignment="Center"/>
  </Grid>
</UserControl>
